// 测试函数调用和递归
function fib(n: number): number {
    if (n <= 1) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

function main(): void {
    let result: number = fib(5);
    console.log("fib(5) = ");
}
